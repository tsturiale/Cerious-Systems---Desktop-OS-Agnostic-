Cerious local credentials folder.

Runtime credentials should live here for local testing and should not be committed to Git. The default Google service-account placeholder path is `data/credentials/google-service-account.json`.
